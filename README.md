# cypress-boilerplate-starter

## Installation 

```bash
npm init -y

npm i cypress --save-dev

```

Run node_modules/.bin/cypress open

files will be created

We can use npx serve


## Run

```bash
"scripts": {
    "e2e": "cypress open"
  },
```

npm run e2e
